# .env는 .gitignore - 커밋 안 됨

KAKAO_MAP_API_KEY=
KAKAO_REST_API_KEY=

PORTONE_STORE_ID=
PORTONE_CHANNEL_KEY=
# 100원 인증 테스트: PORTONE_USE_TEST_CHANNEL=true
PORTONE_CHANNEL_KEY_KAKAOPAY=
PORTONE_CLIENT_KEY=

# API 인증서 핀닝 (운영 API 호스트 SNI — apiBaseUrl과 동일 호스트명으로 추출)
# openssl s_client -connect <호스트>:443 -servername <호스트> 2>/dev/null | openssl x509 -pubkey -noout | openssl pkey -pubin -outform DER | openssl dgst -sha256 -binary | base64
API_CERT_PIN=
